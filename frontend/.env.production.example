# Frontend production env for Cloudflare Workers (OpenNext).
# Copy to `.env.production` and set to your deployed backend base URL (no trailing slash).
# Next.js inlines NEXT_PUBLIC_* at build time, so this must be set BEFORE `npm run cf:deploy`.
NEXT_PUBLIC_API_URL=https://agent-invest-backend.onrender.com
